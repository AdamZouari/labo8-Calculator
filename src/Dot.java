public class Dot extends NumberCreation{


    public Dot(State state){
        super(state);
    }

    @Override
    String create() {
        return null;
        /*
        * if(!state.getCurrentVal().contains(".")) {
            state.appendVal(dot);
        }
        * */
    }
}
